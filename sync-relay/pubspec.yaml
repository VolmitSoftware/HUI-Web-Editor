name: gloss_sync_relay
description: Capability-secured relay for Gloss server and web-editor sync.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  crypto: ^3.0.7
  ffi: ^2.1.4
  shelf: ^1.4.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
